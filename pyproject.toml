[project]
name = "crm-reporting-dashboard"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openpyxl==3.1.5",
    "pandas==3.0.3",
]

[tool.vercel]
entrypoint = "api.generate:handler"
